# Eingent 核心方针与开发目标

## 定位

**手机端工作流的最短路径。**

Eingent 是一个 Android 原生 Agent 调度器，目标是让专业工作任务在手机上以最小摩擦完成。不是让生活更方便，是让工作更高效。

---

## 核心方针

### 一、工作触达零界面
通过 Android 原生机制（Share / Intent / 通知回复）触发 Agent，用户无需打开 App 再操作——工作在哪，Agent 就在哪。

### 二、任务自动分解
用户描述目标，Eingent 拆成子任务分发给合适的 Agent，结果自动聚合回来。调度复杂度对用户透明。

### 三、最少输入，最大产出
每次交互的认知负担最小化。不是"告诉我你要做什么"，而是"你在做什么，我来接管复杂部分"。

---

## 阶段一：初期目标（v0.1.x）

目标：从空壳变为一个可用的工作工具。

| # | 目标 | 验收标准 |
|---|------|----------|
| 1 | **接入 Claude API** | App 能真正调用 LLM，返回有意义的响应 |
| 2 | **基础对话界面** | 简洁的聊天 UI，支持文字输入与结果展示 |
| 3 | **Share Intent 接入** | 从任意 App 分享文本 → Eingent 接收并处理 |
| 4 | **完整工作流 Demo** | 分享一段内容 → Agent 处理（如摘要/翻译/整理）→ 结果可复制/分享 |

阶段一不做多 Agent 编排，先跑通"单 Agent + 原生触发"的完整链路。
